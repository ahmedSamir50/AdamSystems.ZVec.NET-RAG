# Sample 04 — Air-gapped enterprise RAG

ASP.NET SSE endpoint with `FakeChatClient` for CI. Set `ZVEC_LLAMA_MODEL` for optional LLamaSharp (H-LS-WRAP / Story 4.1.1).

```bash
dotnet run --project samples/04-airgapped-enterprise-rag/ZVec.Rag.Sample04.csproj
```
