# ZVec RAG MAUI Blazor Hybrid

Retrieve-only placeholder. Opens the vector index off the UI thread via `BackgroundCollectionOpener`.

Not built in ubuntu CI. Requires MAUI workloads on your dev machine.

```bash
dotnet build -f net8.0-android
```
